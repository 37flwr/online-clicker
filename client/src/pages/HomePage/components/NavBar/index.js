import "./styles.scss";

const NavBar = () => (
  <header className="header-home-page">
    <h1 className="header-logo-container">Spooky Clicker</h1>
    <p className="tagline">Click on monsters to defeat them</p>
  </header>
);

export default NavBar;
